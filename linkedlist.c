#include<stdio.h>
#include<conio.h>
#include<stdlib.h>

struct linkedlist
{
 int data;
 struct linkedlist *next;
}*temp,end=NULL,start=NULL;
int main()
{
 node=(node)malloc(size of(struct linkedlist));
}
