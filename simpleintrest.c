#include<stdio.h>
#include<conio.h>
int main()
{
 float p,r,t,si;;
 printf("Enter the values of principple amount,rate and time");
 scanf("%f%f%f",&p,&r,&t);
 si=(p*t*r)/100;
 printf("Simple interst is =%f",si);
 return(0);
}
